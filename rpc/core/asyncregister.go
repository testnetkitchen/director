package core

// Todo: Copy or extract Register function and implement async registration
/*
	// Async registration
	stateMachine.SendMessage(&state.RegisterValidator{
		ChainID: chain_id,
		Validator: store.ValidatorConfig{
			NetAddress: listen_addr,
			Name:       name,
			PubKey:     pub_key,
		},
	})
*/
